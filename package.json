{
  "name": "trademark-secure-co-mern",
  "version": "1.0.0",
  "description": "MERN Stack implementation for Trademark Secure Co.",
  "main": "server.js",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend --legacy-peer-deps",
    "kill-port": "lsof -ti:5002 | xargs kill -9 2>/dev/null || true",
    "backend": "npm start --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "npm run kill-port && concurrently \"npm run backend\" \"npm run frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
