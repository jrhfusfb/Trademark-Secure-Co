# Copy this file to .env and fill in your values

# Set this to your Vercel backend API URL (only needed if deploying frontend separately)
# When deploying as a monorepo on Vercel, leave this EMPTY — /api calls resolve automatically
# Example for separate deployment: https://your-project.vercel.app
VITE_API_URL=
